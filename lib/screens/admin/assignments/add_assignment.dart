import 'package:flutter/material.dart';

class AddAssignments extends StatelessWidget {
  const AddAssignments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Assignment'),
      ),
    );
  }
}
